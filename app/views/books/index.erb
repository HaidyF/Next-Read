<table>
  <tr>
    <th>Books</th>
  </tr>

  <% @books.each do |book| %>
    <tr>
      <td><%= book.name %></td>
      <td><%= book.review %></td>
       <td><%= book.rating %></td>
    </tr>
  <% end %>
</table>